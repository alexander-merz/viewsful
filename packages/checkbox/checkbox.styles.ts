import { css } from 'lit'

export default css`
  :host {
    font-family: sans-serif;
    color: blue;
  }

  label {
    display: flex;
    align-items: center;
    gap: 0.2rem;
    user-select: none;
  }
`
